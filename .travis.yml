notifications:
  email:
    - eric.crawford@mail.mcgill.ca

language: python
python:
  - 3.5

os: linux
dist: trusty
sudo: false

branches:
  only:
    - master

git:
  depth: 3

cache:
  apt: true

before_install:
  - pip install -r requirements_dev.txt

install:
  - pip install -e .

script:
  - dps-run simple_addition a2c --max-steps=201 --render-step=0